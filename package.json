{
  "name": "jlaw-workhub",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian + Notion + Google Calendar + Slack 통합 업무관리 봇 (Vercel serverless)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "vercel dev"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/node": "^5.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@vercel/functions": "^1.6.0"
  }
}
